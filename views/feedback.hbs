<!DOCTYPE html>
<html>
<head>
    <title>Thank you for your feedback!</title>
    <style>
        body {
            background-color: #f2f2f2;
        }
        .container {
            width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .feedback {
            width: 500px;
            height: 500px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            padding: 20px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .feedback img {
            width: 100px;
            height: 100px;
        }
        .feedback h1 {
            font-size: 2rem;
            margin: 0;
            margin-bottom: 10px;
        }
        .feedback p {
            font-size: 1.2rem;
            margin: 0;
            margin-bottom: 10px;
        }
        .feedback form {
            width: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .feedback form input {
            width: 100%;
            height: 40px;
            border: 1px solid #ccc;
            border-radius: 5px;
            padding: 10px;
            margin-bottom: 10px;
        }
        .feedback form textarea {
            width: 100%;
            height: 100px;
            border: 1px solid #ccc;
            border-radius: 5px;
            padding: 10px;
            margin-bottom: 10px;
        }
        .feedback form input[type="submit"] {
            width: 100px;
            height: 40px;
            border: none;
            border-radius: 5px;
            background-color: #4caf50;
            color: #fff;
            font-size: 1.2rem;
            cursor: pointer;
        }

        /* Close text in grey. Aligned to the right */
        .close {
            color: #aaa;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="feedback">
            <img src="/images/checkmark.svg" alt="checkmark">
            <h1>Thank you for your feedback!</h1>
            <p>Thank you for helping us improve our podcast.</p>
            <p>Feel free to send us additional feedback using the form below.</p>
            {{#if numberOfComments}}
                <p>So far we received {{numberOfComments}} comment(s) for this episode.</p>
            {{/if}}
            <form action="/comments/{{episodeId}}" method="post">
                <input type="email" name="email" placeholder="Your email (optional)">
                <textarea name="comment" placeholder="Speak your mind!"></textarea>
                <input type="submit" value="Send">
            </form>
        </div>
        <div class="close">
            <p>Alternatively you can close this window now.</p>
        </div>
    </div>
</html>
